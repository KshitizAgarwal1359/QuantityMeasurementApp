namespace QuantityMeasurement.Models
{
    // UC17: OperationType enum — equivalent of Spring OperationType enum.
    // Represents the types of operations supported.
    public enum OperationType
    {
        COMPARE,
        CONVERT,
        ADD,
        SUBTRACT,
        DIVIDE
    }
}
